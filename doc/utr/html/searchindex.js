window.USCRIBE_SEARCH = [{"file":"utr24.html","body":"1. Introduction This report describes the Unicon source preprocessor: how a file plus includes becomes the token stream the compiler scans, what the  $  directives do, and the three extensions added in  uni/unicon/preproce.icn  -- triple-quoted strings, function-like macros, and built-in  assert  /  assert_not . It is formatted as a Unicon Technical Report  . The language-facing command list lives in  Programming with Unicon  (language reference, \"Preprocessor\") and UTR #8  ; that text still states that  $define  does not take arguments. Automated tests live under  tests/unicon/  ( triple_strings.icn ,  macros*.icn ,  assert_*.icn ).  unicon -E  writes preprocessed source. The preprocessor is not optional. Every compilation runs it. 2. Motivation and prior art The implementation is Bob Alexander's Jcon preprocessor, brought into Unicon with small edits  . It is line-oriented: it recognizes directives and identifiers, respects string and comment boundaries, and performs textual substitution. It is not a parser. The Jcon baseline provided object-like  $define ,  $include ,  $ifdef  /  $ifndef  /  $else  /  $endif ,  $error ,  $line  /  #line , and predefined symbols from  &features . The implementation-book section \"The Unicon Preprocessor\"   documents that baseline (generator interface, include and  $ifdef  stacks, quoted-string skipping, underscore continuation). It still says macros have no parameters. The language reference says the same  . Three gaps in that baseline are filled here: Multiline string literals.  Unicon string literals are one physical line unless continued with a trailing  _ . A triple-quoted form lets a literal span lines, including text that looks like comments or  $  directives, without changing the lexer grammar. Parameterized macros.  Object-like  $define  cannot abstract repeated call-shaped text. Function-like  $define  adds a parameter list and a replacement body, with C-style    continuation on the definition and call arguments that may span lines. Assertions.  Debug and test builds need a check that disappears entirely in release. Built-in  assert  /  assert_not  expand to runtime tests under  __debug__  /  __test__ , and to nothing otherwise. 3. Architecture Canonical source is  uni/unicon/preproce.icn .  uni/parser/preproce.icn  is a shorter, separate copy used by the standalone parser toolkit; it does not include the extensions in this report. 3.1 Public procedures Procedure Role preprocessor(fname, predefined_syms) Main entry. Initializes state, loops over input, emits preproc(dummy, args) Wrapper:  suspend preprocessor(args[1], predefs()) . predefs() Table of predefined symbols ( _UNIX ,  __DATE__ , preproc_new()  initializes;  preproc_done()  clears large tables so they are not retained across compilations.  preproc_install_builtin_macros()  runs from  preproc_new() . If  __test__  is enabled there,  __debug__  is defaulted. 3.2 State record preproc_fmacro(params, body)  holds a function-like macro: parameter names in order and replacement text. Object-like  $define  symbols live only in  preproc_sym_table . Global Purpose preproc_sym_table Object-like macros: symbol to replacement string. preproc_fun_table Function-like macros: symbol to  preproc_fmacro . preproc_builtin_fun_table Names expanded specially ( assert ,  assert_not ). preproc_if_stack ,  preproc_if_state Conditional compilation. preproc_file_stack ,  preproc_file ,  preproc_filename ,  preproc_line Include stack and current input position. preproc_include_set ,  preproc_include_name Included paths; circular includes are errors. preproc_print_line ,  preproc_print_filename Last emitted position, for  #line . preproc_ml_anchor First physical line of a multiline macro call. preproc_nest_level $if  depth at include start; paired on EOF unwind. preproc_word_chars Identifier characters: letters, digits,  _ . preproc_command Current directive name, for diagnostics. preproc_err_count Errors reported. preproc_dollar_or_pound Whether the directive started with  #  or  $ . preproc_fmacro_parse_error Blocks object-like fallback after a failed preproc_assert_uid Suffix for unique temporaries in  assert  expansions. 3.3 Driver preprocessor()  is a generator. The translator drains it into  yyin : every yyin ||:= preprocessor(fname, uni_predefs) do    yyin ||:= \"\n\" in  uni/unicon/unicon.icn .  uni_predefs  is  predefs()  plus  -D  options. After the generator finishes, a non-zero  preproc_err_count  or a non-empty  parsingErrors  aborts before  yyparse() . Each line from  preproc_read()  is scanned under  line ? { ... } . After leading  preproc_space() : #line  (optional spaces, GCC form) goes to  preproc_scan_directive() . $  followed by a character in  nonpunctuation  (letters, digits, space, tab, form feed, carriage return) goes to  preproc_scan_directive() . Otherwise  &pos := 1  and the line goes to  preproc_scan_text() . Unicon directives are  $ -prefixed. A  #  that is not  #line  is not a directive. With macros loaded,  preproc_scan_text()  treats  #  outside strings as a comment to end of line. Do not rely on C-style  #define  at column 0. There is no second pass over the same line. 3.4 Conditional compilation /preproc_if_state  is Unicon's null test. After  $ifdef , the true branch sets  preproc_if_state  to null (emit); the false branch sets  \"false\"  or  \"off\"  (skip). So  /preproc_if_state  means the current region is active (or there is no  $ifdef ).  preproc_if_state  is the nonnull test. $define ,  $undef ,  $include , and text expansion run only when  /preproc_if_state  succeeds.  preproc_scan_text()  returns without emitting in skipped regions -- no triple-string rewrite, no macro expansion.  $ifdef  /  $ifndef  /  $else  /  $endif  still run in skipped regions so nested conditionals stay balanced. A bare  if  token inside skipped text is mapped to  $if , which pushes state and forces  \"off\" . 3.5 Text scan order When the region is active,  preproc_scan_text() : Rewrites  \"\"\"  ...  \"\"\"  via  preproc_rewrite_triple_strings()  before identifier scans, so the body is never treated as directives or macros. If either symbol table is non-empty, scans for  # , strings, and identifiers. Otherwise it emits the line after  preproc_sync_lines() . Looks up function-like macros before object-like. A name in  preproc_fun_table  is a call only if  preproc_expand_fmacro_call()  sees  (  after optional space; otherwise  &pos  is restored and the name may still match an object-like macro. Rescans replacements through  preproc_scan_text(done_set) .  done_set  is  &null , a string, or a set of names on the current expansion chain; it blocks infinite recursion. String literals  \"...\"  /  '...'  are skipped with escape and trailing- _  continuation ( preproc_read() , adjusting  preproc_print_line ). 4. Directives preproc_scan_directive()  reads the command with  preproc_word() . Unknown names in an active region call  preproc_error(\"unknown preprocessor directive\") . Command Behavior define Function-like  name(params...) body  via undef Removes the name from both tables and from the builtin ifdef  /  ifndef Pushes  preproc_if_state ; sets state from $if Placeholder for  if  tokens inside excluded code: else  /  endif Pop, toggle, or close the branch; error on mismatch. include Quoted or bare filename via  preproc_qword() ; circular line Sets  preproc_filename  /  preproc_line  from a error User error; message to end of line or  # . ITRACE Sets  &trace  from an integer. C Accumulates lines until  $Cend ; passes the text to 5. Triple-quoted multiline strings Delimiters  \"\"\"  ...  \"\"\"  mark a string whose body may span physical lines. The preprocessor collects raw text -- including lines that look like comments or  $  directives -- and rewrites the region to an ordinary quoted literal. The lexer grammar in  uni/parser  is unchanged. procedure main()    local x    x := \"\"\"line one line two\"\"\"    write(x) end Output: line one line two The opener may sit on the same line as code or alone on a following line. Indentation and newlines in the body are kept. Multiline bodies do not shift  &line  for surrounding statements ( triple_strings.icn ). procedure main()    local x    x := \"\"\" $ifdef __debug__ inside triple $endif \"\"\"    write(\"[\", x, \"]\")    x := \"\"\"escaped triple: \"\"\" inside\"\"\"    write(x)    x := \"contains \"\"\" literally\"    write(x)    x := \"\"\"ends-with-backslash\"\"\"    write(\"[\", x, \"]\") end Output: [ $ifdef __debug__ inside triple $endif ] escaped triple: \"\"\" inside contains \"\"\" literally [ends-with-backslash] Inside  \"...\"  or  '...' , a  \"\"\"  sequence is copied through and does not start this mode. To put three double quotes in a triple-quoted body, write  \"\"\"  (backslash before each quote). The four-character form  \"\"\"  is not an escape, so a body may end with a single   . $ifdef  /  $endif  and  #  comments inside the body are string content. Continuation lines pulled for an open triple are never scanned as directives. preproc_rewrite_triple_strings()  runs at the start of  preproc_scan_text() , only in an active region, and only if the current line contains  \"\"\" . Outside quotes it still honors  #  as a comment to end of that physical line. After an opener, it accumulates a body until the closer; if the line ends first, it appends a newline,  preproc_read() s the next line, and continues. An unclosed delimiter at EOF is  unterminated triple-quoted string . The body is turned into one ordinary string with  preproc_quote_string() : named Unicon escapes for specials (including DEL as  \u007F ), backslash and quote escaped, other C0 bytes as three-digit octal via  preproc_octal_escape() .  triple_strings.icn  checks that a DEL byte in a triple string survives compilation ( ord  127). Tests:  tests/unicon/triple_strings.icn ,  tests/unicon/stand/triple_strings.std . 6. Function-like macros Function-like macros extend object-like  $define  with a parameter list and a replacement body. Nested calls, zero arguments, and commas inside quoted arguments are supported ( macros.icn ): $define SUM(a,b) ((a) + (b)) $define TWICE(x) SUM(x,x) $define ZERO() 0 procedure main()    write(SUM(2, 3))    write(TWICE(5))    write(ZERO()) end Output: 5 10 0 Quoted commas and parentheses do not split arguments. A call may continue on the next line without   : $define ID(x) x $define WRAP3(a,b,c) ((a) || \"|\" || (b) || \"|\" || (c)) $define SUM(a,b) ((a) + (b)) procedure main()    write(ID(\"a,b\"))    write(WRAP3(\"a,b\", \"c(d)\", \"e\"))    write(SUM(1,       2)) end Output: a,b a,b|c(d)|e 3 On  $define ,  (  must follow the name immediately.  $define SUM (a,b) ...  is object-like: the value starts at the space. At a call site, space before  (  is allowed ( preproc_opt_space() ), so a body may write  SUM (x,y) . Zero parameters are legal ( ZERO() ). Whitespace-only between  (  and  )  is zero arguments ( ZERO( ) ), not one empty argument. The replacement may continue on following lines if each continued line ends with    as the very last character (no trim).  preproc_scan_define_value()  and  preproc_define_value_next_line()  splice those lines: the    and newline are dropped; other spaces are kept. A    with trailing spaces is not a continuation. Leading space is skipped only before the first line of the body. The same    rule applies to object-like  $define  values ( macros_define_continuation.icn ). EOF after a splice is  unterminated $define (line continuation) . $define GREETING \"hello \" ||  \"there\" $define SUMCONT(a,b) ((a) +  (b)) procedure main()    write(GREETING)    write(SUMCONT(2, 3)) end Output: hello there 5 A call  NAME(arg1, ...)  may span physical lines without   .  preproc_scan_macro_args()  pulls more lines via  preproc_macro_arg_next_line()  until the closing  ) . A backslash inside a call is ordinary argument text ( macros_call_backslash_literal.icn ), not a  $define  splice. Arguments split on top-level commas, with parenthesis depth, and with  \"...\"  /  '...'  opaque (commas and parens inside quotes do not split).  preproc_subst_fmacro()  replaces whole identifiers in the body, not substrings, and does not substitute inside strings. The result is rescanned, so  TWICE  and chains such as  A  ->  E  expand in one pipeline.  done_set  blocks expanding the same name again on that chain. Empty arguments ( SUM(1,) ) are  empty macro argument . Duplicate parameter names are  duplicate parameter name in $define . Arity must match ( wrong number of args in macro call to NAME ). Unclosed  (  is  unterminated macro invocation . If  (  follows the name on  $define  but the parameter list is malformed,  preproc_fmacro_parse_error  is set so the line is not treated as a bad object-like  $define . Call continuation anchors  preproc_print_line  to the first physical line of the call ( preproc_ml_anchor ).  &line  inside the expansion is that line; the next source line after the call skips the continuation rows.  $define     splicing does not adjust  preproc_print_line .  &line  in a multiline body is the invocation line, not each physical line of the definition ( macros_define_multiline_line.icn ). $define ASLINE(x) x procedure main()    write(&line)    write(ASLINE(&line    ))    write(&line) end Output: 4 5 7 (The call starts on line 5; line 6 is the continuation; the next statement is line 7.) A function-like name is defined for  $ifdef .  $undef  removes it. A name is either function-like or object-like, not both;  $define  updates one table and clears the other. Redefinition of a function-like name is an error unless  preproc_same_fmacro()  finds the same parameters and body. $define SUM(a,b) ((a) + (b)) $ifdef SUM $define HAS_SUM 1 $else $define HAS_SUM 0 $endif $undef SUM $ifdef SUM $define STILL 1 $else $define STILL 0 $endif procedure main()    write(HAS_SUM)    write(STILL) end Output: 1 0 $undef assert  then  $define assert(...)  replaces the builtin;  macros.icn  does this for both  assert  and  assert_not . Tests:  tests/unicon/macros.icn ;  macros_multiline_line.icn ,  macros_define_multiline_line.icn ,  macros_define_continuation.icn ,  macros_call_backslash_literal.icn ; negatives under  tests/unicon/data/macros_bad_*.icn  and  macros_bad_define_eof*.icn . 7. Built-in ``assert`` and ``assert_not`` preproc_install_builtin_macros()  registers  assert  and  assert_not  in  preproc_fun_table  as placeholder  preproc_fmacro  records and marks them in  preproc_builtin_fun_table . Expansion is not  preproc_subst_fmacro() :  preproc_expand_fmacro_call()  builds a Unicon fragment from the argument text. Arity is one or two:  assert(expr)  or  assert(expr, label) . Any other count is  wrong number of args in macro call . Semantics follow Unicon success and failure, not Boolean truth:  assert(expr)  expects  expr  to succeed;  assert_not(expr)  expects  expr  to fail.  assert  is alternation ( expr | { ... } );  assert_not  is  if (expr) then { ... } . Mode Enable On failure Debugging $define __debug__  or  -D__debug__ runerr(219, payload) . The process stops. Traceback Testing $define __test__  or  -D__test__ write  of  [file:line] assertion failed (expr) Release neither Expands to nothing ( assert_strip.icn ). If both  __test__  and  __debug__  are defined, the testing path ( fail ) wins.  preproc_new()  and a later  $define __test__  both default  __debug__  when it is absent. These macros are standalone statements. Disabled expansion is empty, so expression positions such as  if assert(...)  are not supported. The debug payload is the quoted expression text, then  char(30)  and the label if the label is non-empty ( AssertRunerrSep  in  errmsg.r ).  assert_not  prefixes  char(29)  so the traceback can print  assert_not( . Error 219 is  assertion failed  ( data.r ).  errmsg.r  skips the ordinary  Run-time error 219  banner and the offending- value line; it prints  [file:line] assertion failed: ... . If the label expression fails, the assertion diagnostic still runs: the expansion does  lbl := ((label) | \"\")  ( assert_failing_label.icn ). Each expansion increments  preproc_assert_uid  so generated temporaries ( __preproc_assert_N_lbl ) do not collide -- icont has no block-local variables in expression-level  {...}  compounds. The fragment is rescanned so nested macros in the inserted code expand. $undef assert  then  $define assert(...)  replaces the builtin like any user function-like macro. $define __debug__ procedure main()    local x    x := 1    assert(x = 1)    assert_not(x = 2)    assert(2 === 1 + 1, \"1 + 1 = 2\")    write(\"ok\") end Output: ok A failing debug assert stops the process. The traceback names  assert  /  assert_not  rather than  runerr : $define __debug__ procedure main()    assert_not(1 = 1)    write(\"should not reach here\") end Output: [assert_ex.icn:3] assertion failed: 1 = 1 Traceback:    main()    assert_not(1 = 1) from line 3 in assert_ex.icn Under  __test__ , failure writes a diagnostic and  fail s, so later statements still run: $define __test__ procedure main()    assert(1 = 2, \"keep going\")    write(\"after fail\")    assert(1 = 1)    write(\"done\") end Output: [assert_ex.icn:3] assertion failed (1 = 2) keep going after fail done With neither symbol, the calls vanish. The asserts below would fail at run time if they were still present, so reaching  write()  is the proof they were stripped: procedure main()    local x    x := 1    assert(x = 2)    assert_not(x = 1)    write(\"strip-ok\") end Output: strip-ok Tests:  assert_debugging.icn  (passing cases, then a failing assert five frames down),  assert_testing.icn ,  assert_strip.icn ,  assert_failing_label.icn ,  assert_not_failing.icn , matching  stand/*.std . Builtin override is in  macros.icn . Concern Primary routines ( uni/unicon/preproce.icn ) Triple-quoted strings preproc_rewrite_triple_strings() , Function-like macros record preproc_fmacro , Assertions preproc_install_builtin_macros() , 8. Line numbers, errors, and includes preproc_sync_lines()  emits  #line N \"file\"  when the filename changes or the gap is large; for small gaps it may emit blank lines.  preproc_scan_text()  can  suspend  more than once per logical line. Features that insert, delete, or merge lines must keep  preproc_line  and  preproc_print_line  consistent with that. preproc_error(msg)  builds  File ...; Line N # $directive: ... , increments  preproc_err_count , and pushes a  ParseError  onto  parsingErrors . preproc_read()  increments  preproc_line  and returns the next line from  read()  or from a list \"file.\" On EOF it checks  preproc_if_stack  depth against  preproc_nest_level , closes the file, pops  preproc_file_stack , and removes the path from  preproc_include_set . String continuation, triple-quoted bodies, and macro arguments that need another physical line all call  preproc_read()  again. preproc_quote_string(s)  escapes arbitrary text for a Unicon string literal.  preproc_istring()  /  preproc_istring_radix()  decode escapes in quoted filenames. 9. Extending the preprocessor All of the following is in  uni/unicon/preproce.icn  unless noted. Coordinate any change with  uni/parser/preproce.icn  if that copy is still shipped; it does not currently match. 9.1 Kind and touchpoints Kind Touchpoints New or changed  $  directive preproc_scan_directive() : add a  case  label; set Object-like macro behavior preproc_sym_table , substitution in Function-like macro behavior preproc_fun_table , Built-in special function macro preproc_install_builtin_macros() , Source syntax rewritten to plain Unicon A phase in  preproc_scan_text() . Must respect New predefined symbol predefs() . Symbols that depend on compiler options 9.2 Guards and line accounting Directives that define, undefine, or include use  if /preproc_if_state then  so they do not run when code is stripped. Directives that only keep parser state in skipped regions follow  ifdef  /  $if  /  else  /  endif , not that guard. If the feature reads extra lines or changes output length, check  preproc_line ,  preproc_print_line , and  preproc_sync_lines() .  unicon -E  shows the emitted  #line  stream. Multiple  suspend s per input line must leave  preproc_print_line  consistent at the end of  preproc_scan_text() . 9.3 Errors and tests Use  preproc_error(msg)  after  preproc_command  is set so the message includes  $directive: . The compiler exit path is  preproc_err_count  and  parsingErrors  in  unicon.icn . Add a driver under  tests/unicon/  and expected output under  stand/ . Error cases belong in  tests/unicon/data/ . Update   when user-visible semantics change. 9.4 New ``$`` directive After  preproc_command := preproc_word() , the  case preproc_command of  dispatch matches the word after  $ . A new label: \"mydir\": {    if /preproc_if_state then {       # parse remainder of line; on failure:       preproc_error(\"optional detail\")    } } Unknown names still hit  default  and  preproc_error(\"unknown preprocessor directive\")  when active. 10. Related documents Document Content UTR #15  How to write and submit a Unicon Technical Report. UTR #8  Language reference preprocessor commands Implementation book  doc/ib/p3-unicon.tex , \"The Unicon Preprocessor\": Other  doc/ib/  preprocessor mentions (Idol, Ratfor, rtt) are different tools, not  preproce.icn . References ","title":"UTR 24: The Unicon Preprocessor"},{"file":"utr26.html","body":"1. Introduction This report describes the current design and implementation of native SSH in the Unicon runtime: how a session is opened, how additional channels and SFTP handles are derived from it, how stream I/O, ordered events, and metadata operations are exposed, and what remains out of scope. It is formatted as a Unicon Technical Report  . The language-facing reference lives in  Programming with Unicon  (Chapter 6, \"Secure Shell\") and the language reference ( open  mode  h ,  Attrib ,  receive ,  stat  /  remove  /  rename ).   collects examples by mode and use case. Automated tests live in  tests/posix/ssh.icn . The feature is optional. A build with libssh present reports  secure shell  in  &features  (the  _SSH  feature flag). Without the library, mode  h  is ignored and the SSH paths are compiled out, as with other optional features. 2. Motivation and prior art Unicon programs that needed SSH previously relied on  popen()  or pipes to an external  ssh  process. That is not portable (Windows often has no OpenSSH client in  PATH ) and gives the program no structured access to channels, exit status, or SFTP. Native primitives let an application authenticate to hosts, run commands, and transfer files without an external process. 2.1 Library choice Two C libraries were considered.  libssh  (LGPL) supports both client and server, modern key exchange and host keys, and true non-blocking I/O via  poll(2) .  libssh2  (BSD) is client-only and cannot be used fully non-blocking. The implementation binds  libssh   , for server-side optionality later and for the callback API that preserves stdout/stderr arrival order ( ). A full protocol reimplementation (Paramiko, Go's  x/crypto/ssh ) would mean owning crypto primitives and protocol-drift / CVE tracking indefinitely. Binding to libssh via the existing C runtime avoids that. 2.2 Open as the verb Python (Paramiko) and Go ( x/crypto/ssh ) both treat \"new session\" as sugar over \"open another channel on this connection.\" The verb is  open , applied to an existing connection object, not \"clone\" or \"duplicate.\" Unicon already has  open()  as the universal constructor for files, sockets, SSL, and messaging connections.  Clone()  is the Graphics-facility naming convention; a distinct  channel()  function would add a new global name for an operation  open()  already expresses. 2.3 Why not Messaging The Messaging facility (HTTP/POP/SMTP,  rmsg.r , libtp)   is not a fit. Messaging's transport-discipline abstraction has the same 1:1 single-stream limitation as SSL -- no channel-multiplexing concept -- so moving into that world would not solve the multiplexed-channel requirement, only relocate it. SSH's request/channel model ( exec  /  shell  /  sftp  as concurrent channels) also does not map onto Messaging's request/response verb model ( GET  /  POST  /  RETR ). SSH does not route through  ssh://  URIs or  Mopen() . It uses the  h  mode character and plain  user@host  string parsing, independent of the Messaging/URI subsystem. 3. Architectural grounding The implementation follows established SSL and socket patterns rather than inventing a parallel subsystem. union f  in  src/h/rstructs.h  is the file block's descriptor slot: a tagged union of  FILE * , socket  fd ,  SSL *  when  HAVE_LIBSSL , and so on, discriminated by status bits at runtime. SSH does  not  embed libssh objects directly in the union. It adds a pointer,  struct SSHfile *sshf , matching the Messaging  MFile *  precedent.  SSHfile  is allocated with  malloc  so pointers to it are stable across garbage collection, and is freed by the close hook, never by the collector. A malloc'd block is required once the session must hold a list of child channels whose addresses survive compaction ( ). Status bits live in  src/h/rmacros.h .  Fs_Encrypt  is  0200000000 ; SSH takes the next bit,  Fs_SSH = 0400000000 .  open() 's mode string is scanned character-by-character in  src/runtime/fsys.r .  e  sets  Fs_Encrypt . SSH uses  h  /  H  ( s  is already the Messaging short-request flag). Attributes are  \"key=value\"  strings passed as trailing arguments to  open() , parsed in  create_ssh_session()  the same way  create_ssl_context()  parses SSL attributes -- not a fixed argument list. A leading integer, when present, is a connect timeout in milliseconds, as for plain sockets. All stream I/O funnels through one dispatch point:  u_read()  /  u_write()  in  src/runtime/rposix.r , which already branch on  Fs_Socket  /  Fs_Encrypt . SSH adds a third branch for  Fs_SSH , reading from the arrival-order queue ( ) or  sftp_read() , and writing via  ssh_channel_write()  or  sftp_write() . Nothing above this dispatch ( reads() ,  read() , and so on) needs its own SSH case, except where a line-oriented helper must not wait for a newline that will never arrive ( ). Close is  explicit, not GC-driven . The close hook in  fsys.r  frees SSL resources directly; there is no finalizer during garbage collection. SSH files follow the same convention -- leaking a channel or session means not calling  close() , the same as any other file. receive()  already returns a general-purpose  posix_message  record (fields  addr  and  msg ) for UDP and raw datagrams. SSH reuses that type for an ordered stdout/stderr/exit-status event stream ( ). stat(f)  already  type_case s on its argument (string path versus open file). SFTP extends both branches ( ).  remove()  and  rename()  gain an optional leading session argument, matching the existing Unicon idiom that many functions take an optional leading file or window ( write(\"hello\")  versus  write(f, \"hello\") ). 4. Connection and channel model 4.1 ``open()`` dispatch Two shapes, both using the existing  open() . The second argument is the usual mode string;  h  is a new character in that scan, and  c  /  s  after  h  select a channel or SFTP the same way  u  after  n  selects UDP ( n  is TCP,  nu  is UDP). open(host_or_\"user@host\", \"h...\", attrs...)  -- first argument is a string: opens a new authenticated SSH  session  and, by default, an interactive shell channel on it ( ). open(s, \"hc\"/\"hs\", attrs...)  -- first argument is an existing SSH session or channel file: opens a  new channel  ( hc ) or SFTP handle ( hs ) on the same underlying session (reusing the transport and authentication, no new handshake). Opening on a channel opens a  sibling  on the session owner, not a child of the channel. A session opened with  channel=no  has no channel of its own ( Fs_SSH  alone, no  Fs_Socket ). It exists so later  open(s, \"hc\")  /  open(s, \"hs\")  calls have an authenticated transport.  channel=yes  is the default. Combining  channel=no  with mode  hc  or  cmd=  is contradictory and fails with a bad-attribute error. s := open(\"jafar@\" || host, \"h\", \"key=\" || keypath) write(s, \"show version\n\") while line := reads(s) do write(line) close(s) s := open(\"user@host\", \"h\", \"key=id_ed25519\", \"channel=no\") c1 := open(s, \"hc\", \"cmd=uname -a\") c2 := open(s, \"hc\", \"cmd=uptime\") 4.2 Host string The first argument is  host ,  user@host , or either with  :port . The port uses the same  host:port  form as socket  open() ; IPv6 uses  [addr]:port . A bare IPv6 literal is not misparsed: an unbracketed host is split on  :  only when there is a single colon. There is no  port=  attribute. user@host  wins over a  user=  attribute. If neither is given, libssh's default user (the local login name) is used. ssh://user@host  URI syntax is not used ( ). 4.3 Default channel: shell, not bare session open(host, \"h\", ...)  with no  cmd=  and without  channel=no  opens a shell on a remote PTY immediately, so the common interactive case is one handle. The PTY is requested with  TERM  (from  term= , else  $TERM , else  xterm ) and a window size (from  cols=  /  rows= , else the local tty via  TIOCGWINSZ , else 80x24). Without a real size, remote  ls(1)  and friends fall back to one column. A shell channel has no clean command boundaries: prompt text, echoed input, and output all interleave. For scripted use, prefer mode  hc  with  cmd= , which has clean stdout and a real exit status: c := open(s, \"hc\", \"cmd=show version\") 5. Attributes and authentication Trailing  open()  arguments are  name=value  strings. Empty values and unknown names fail (error 1321). Boolean-style attributes without a value are not accepted -- every attribute must contain  = . SFTP is selected by mode character  s  after  h  ( \"hs\" ), not by a valueless attribute. Attribute Meaning Notes user= Remote login name Overridden by  user@host key= Path to a private key file Same convention as SSL's  key= keypass= Passphrase for an encrypted private key Distinct from  password= password= Remote login password hostkeyfile= Path to an OpenSSH  known_hosts  file Passed to libssh as  SSH_OPTIONS_KNOWNHOSTS cmd= Command for a one-shot exec channel Requires mode  hc ; contradicts  channel=no channel= yes  (default) or  no no  is transport-only term= Remote  TERM  for a PTY Default  $TERM  or  xterm cols=  /  rows= PTY size Default from the local tty, else 80x24 keypass=  is deliberately distinct from  password= . SSL's existing  password  attribute means \"key-decryption passphrase,\" a different concept from an SSH login password. Reusing  password=  for both would confuse the two. The  -  mode-character flag disables host-key verification, reusing the existing SSL peer-verification-disable flag ( do_verify ).  Fs_Encrypt  and  Fs_SSH  are mutually exclusive contexts, so there is no conflict. Mode  \"h-\"  is the usual spelling. A leading integer attribute is a connect timeout in milliseconds and is applied as libssh's  SSH_OPTIONS_TIMEOUT  /  TIMEOUT_USEC . The default port is 22 when  :port  is omitted. 5.1 Authentication order If both  key=  and  password=  are given, they are tried in the order the attributes appear in the call, not in a hardcoded priority. If  neither  is given, the implementation falls back to  ssh_userauth_publickey_auto()  -- default identities under  ~/.ssh  and a running agent -- not to password authentication. Agent use through this fallback is supported; an explicit  agent=  attribute and keyboard-interactive auth remain out of scope. key=  does not expand  ~ . Callers (and the  ussh  demo) must expand a leading  ~/  themselves; libssh treats the path literally. 6. Examples and use cases These examples assume a Unicon build with the  secure shell  feature. Connection failures fail with  &errortext  set; the usual form is  s := open(...) | stop(&errortext) . The packaged demo  uni/progs/ussh.icn  is a small client that covers the interactive and one-shot command cases. 6.1 Modes Like every other  open() , SSH uses the second argument as a mode string (it defaults to  \"r\"  when omitted, which is not useful for SSH).  h  /  H  is a mode character in that same scan, alongside  r  /  w  /  n  /  e  /  m . After  h ,  c  selects a command/channel and  s  selects SFTP -- the same \"facility letter, then modifier\" order as  nu  (UDP) and  ms  (Messaging short-request).  n  alone is TCP. Without a preceding  h ,  c  is still create+write and  s  is still ignored (or Messaging short-request after  m ). Trailing  \"name=value\"  arguments are attributes, not modes -- the same slot SSL uses for  key=  and friends. Mode characters combine.  \"h-\"  is SSH plus skip host-key verification (the  -  flag also disables SSL peer checks).  \"hc\"  is a channel (exec when  cmd=  is given, else a shell).  \"hs\"  is SFTP;  r  /  w  /  a  /  b  set transfer intent ( \"hsw\" , or  open(..., \"hs\", ..., \"w\") ). Form Kind Handle open(host, \"h\", ...) mode  h New session plus a PTY shell open(host, \"h-\", ...) mode  h  and  - Same, no host-key check open(host, \"hc\", \"cmd=...\") mode  hc , attribute  cmd= New session plus an exec channel open(host, \"h\", \"channel=no\") mode  h , attribute  channel= Session only (no channel) open(s, \"hc\") mode  hc  on an existing SSH file Extra shell channel on that session open(s, \"hc\", \"cmd=...\") mode  hc , attribute  cmd= Extra exec channel on that session open(s, \"hs\", \"path=...\", \"w\") mode  hs  plus access chars SFTP file or directory open(host, \"hs\", \"path=...\", \"w\") mode  hs  on a host string New session plus SFTP (one handle) leading integer attribute (timeout) Connect timeout in milliseconds channel=no  with  hc  or  cmd=  fails (error 1321).  cmd=  without  c  also fails. 6.2 Interactive shell With no  cmd= ,  open(..., \"h\")  requests a remote PTY and shell.  net::ssh_interactive(s)  attaches the local tty (raw mode,  select()  with  &input , CRLF for display): import net procedure main()    s := open(\"user@host\", \"h\", \"key=/home/user/.ssh/id_ed25519\") |         stop(&errortext)    ssh_interactive(s)    close(s) end term= ,  cols= , and  rows=  override the PTY. Mode  \"h-\"  skips  known_hosts  (useful on a first connect before the key is installed). Password auth uses  password=  instead of, or in addition to,  key= . 6.3 Remote command Mode  \"hc\"  with  cmd=  runs one command and returns its stdout through ordinary  read()  /  reads() .  Attrib(c, \"exitstatus\")  is the remote exit code once it has arrived: procedure main()    s := open(\"user@host\", \"hc\", \"key=/home/user/.ssh/id_ed25519\",              \"cmd=uname -a\") | stop(&errortext)    while write(read(s))    write(\"exit status: \", Attrib(s, \"exitstatus\"))    close(s) end The host string may include a port ( user@host:2222 ) or an IPv6 address ( user@[2001:db8::1]:22 ). A leading integer is a connect timeout in milliseconds: s := open(\"user@host\", \"hc\", 5000, \"cmd=true\") | stop(&errortext) 6.4 Ordered stdout and stderr Stream  reads()  see stdout only. When stdout and stderr must stay in arrival order, or when stderr should go to  &errout , use  receive() : s := open(\"user@host\", \"hc\", \"key=/home/user/.ssh/id_ed25519\",           \"cmd=echo out; echo err 1>&2; exit 3\") | stop(&errortext) while m := receive(s) do    case m.addr of {       \"stdout\" : writes(&output, m.msg)       \"stderr\" : writes(&errout, m.msg)       \"exit\"   : write(&errout, \"[exit \", trim(m.msg), \"]\")       } close(s) Attrib(c, \"stderr\")  is the other stderr path: it drains bytes accumulated since the previous call, without preserving order against stdout. See  . 6.5 Several channels on one session channel=no  opens an authenticated transport. Later  open(s, \"hc\")  /  open(s, \"hs\")  calls add exec, shell, or SFTP handles on that session. Closing  s  invalidates every derived handle: s := open(\"user@host\", \"h\", \"key=/home/user/.ssh/id_ed25519\",           \"channel=no\") | stop(&errortext) c1 := open(s, \"hc\", \"cmd=uname -a\") | stop(&errortext) c2 := open(s, \"hc\", \"cmd=uptime\") | stop(&errortext) while write(read(c1)) while write(read(c2)) close(c1) close(c2) close(s) Opening on an existing channel opens a sibling on the session owner, not a child of that channel. 6.6 SFTP The same session carries SFTP. Mode  \"hs\"  plus  path=  opens a remote file or, if the path is a directory, lists it.  stat ,  remove , and  rename  take the session as a leading argument: s := open(\"user@host\", \"h\", \"key=/home/user/.ssh/id_ed25519\",           \"channel=no\") | stop(&errortext) c := open(s, \"hs\", \"path=/tmp/out.bin\", \"w\") | stop(&errortext) writes(c, ReadBytes(\"local.bin\")) close(c) info := stat(s, \"/tmp/out.bin\") | stop(&errortext) write(\"remote size: \", info.size) d := open(s, \"hs\", \"path=/tmp\") | stop(&errortext) while write(read(d)) close(d) rename(s, \"/tmp/out.bin\", \"/tmp/out.bin.bak\") remove(s, \"/tmp/out.bin.bak\") close(s) 6.7 Concurrent hosts Each connection is blocking. Concurrent hosts use Unicon  thread , one thread per in-flight session: procedure run_cmd(host, key)    local s, line    s := open(host, \"hc\", \"key=\" || key, \"cmd=uname -a\") | {       write(&errout, host, \": \", &errortext)       fail       }    while line := read(s) do write(host, \": \", line)    close(s) end procedure main()    local host, key    key := \"/home/user/.ssh/id_ed25519\"    every host := \"user@alpha\" | \"user@beta\" | \"user@gamma\" do       thread run_cmd(host, key) end Channels that share a session serialize on the session mutex ( ). Separate sessions do not. 7. Error handling Connection and channel failures  fail , they do not  runerr . This matches the SSL connect path: set  &errortext , tear down what was allocated, and fail, so callers write  if s := open(...) then ... else ... . Runtime errors are reserved for type mistakes (passing a non-SSH file to  open(s, \"hc\") , a bad  Attrib  name on an SSH file). Error numbers, defined in  src/runtime/data.r : Number Text Typical cause 1320 SSH error Connect failure, allocation, bad host string 1321 bad ssh attribute Unknown name, empty value,  cmd=  without  c ,  channel=no  with  hc 1322 SSH authentication error No method succeeded 1323 SSH host key verification error Server not in  known_hosts  (when verifying) 1324 SSH channel error Channel open, I/O, or a closed/cascaded handle 1325 SFTP error SFTP subsystem, path, or transfer failure set_ssh_errortext()  appends libssh's own message where available. Blocking libssh calls run under  DEC_NARTHREADS ;  &errortext  is set only after the thread is re-registered, because Unicon allocation is not allowed while unregistered. 8. I/O: streams, ``Attrib()``, and ``receive()`` Three tiers, layered simple-default to full-fidelity. All three share one per-channel event queue populated by libssh callbacks ( ). 8.1 Plain stream I/O read()  /  write()  /  reads()  /  writes()  work unmodified via the existing  u_read()  /  u_write()  dispatch, extended with an  Fs_SSH  branch. Stream reads consume  stdout only . They are enough when the caller does not need to distinguish stderr or preserve cross-stream order. Writes to a channel call  ssh_channel_write()  and retry short writes. Writes to a transport-only session (no channel) fail with SFTP regular files use  sftp_write()  on the same path. 8.2 ``Attrib()`` for exit status, stderr, and EOF Attrib()  already exists generically -- documented as \"read/write attributes (threads, sockets, ...)\" -- and is already used for socket options. SSH channels add three query-only names: Attrib(c, \"exitstatus\")  -- the remote command's exit status, as an integer.  Fails until the exit-status callback has fired.  The implementation pumps the channel until that happens or the channel is done with no status. Preferring callback state over libssh's blocking  get_exit_*  helpers avoids version-sensitive fallbacks once callbacks are registered. This reuses the ordinary Unicon \"fail = not ready\" idiom, the same as  read()  failing at EOF. Attrib(c, \"stderr\")  -- drains  only new bytes since the last call  (a live incremental accessor, not a cumulative snapshot). Not gated on EOF: reading partial stderr mid-run is safe and useful. Returns a possibly empty string. Attrib(c, \"eof\")  --  1  once the remote sent EOF, else  0 . Useful when a caller is multiplexing with  select()  and needs to distinguish \"no stdout yet\" from \"channel finished.\" Unknown names on an SSH file raise 1321. c := open(s, \"hc\", \"cmd=apply-config /tmp/newconfig.json\") while line := reads(c) do process(line) if Attrib(c, \"exitstatus\") = 0 then write(\"ok\") else write(\"failed: \" || Attrib(c, \"stderr\")) 8.3 ``receive()`` for ordered events A command can produce interleaved stdout and stderr where the caller needs to know  when  an stderr message occurred relative to stdout. Neither  reads()  (stdout only) nor  Attrib(c, \"stderr\")  (a separate drain) can preserve that. receive(c)  on an SSH channel yields the next queued event as a  posix_message  whose  addr  is  \"stdout\" ,  \"stderr\" , or  \"exit\"  and whose  msg  is the payload. The exit status is kept as a  string  so  receive() 's return type stays uniform across every kind of file it can be called on; Unicon's  =  already coerces numeric strings. c := open(s, \"hc\", \"cmd=process_records\") while m := receive(c) do    case m.addr of {       \"stdout\" : write(\"OUT: \" || m.msg)       \"stderr\" : write(\"ERR: \" || m.msg)       \"exit\"   : write(\"done, status \" || m.msg)       } receive()  fails once the channel is exhausted. 8.4 Arrival-order queue Preserving cross-stream order requires libssh's  callback API  ( ssh_channel_callbacks , with a  channel_data_function  receiving an  is_stderr  flag) -- not  ssh_channel_poll()  /  ssh_channel_read() , which buffer stdout and stderr separately and lose interleaving. Callbacks are registered  before  the channel is opened so early data cannot land in libssh's own buffers. Each callback appends a tagged chunk ( SSH_CHUNK_STDOUT  /  STDERR  /  EXIT ) to a heap-allocated linked list on the  SSHfile . Callbacks can fire inside blocking libssh calls made while the thread is unregistered ( DEC_NARTHREADS ), where Unicon allocation is not allowed.  ssh_pump()  drives  ssh_channel_poll_timeout()  so the callbacks run. Stream reads consume stdout chunks;  Attrib(c, \"stderr\")  drains stderr chunks;  receive()  pops whatever is at the head. One mechanism, three accessors. 9. Interactive shells and ``select()`` 9.1 Remote PTY An interactive channel requests a PTY before  shell . Size and  TERM  are described in  . The session file is also marked  Fs_Socket  so existing socket dispatch (including  select()  and  get_fd() ) applies. 9.2 Partial-line ``reads()`` Interactive prompts have no trailing newline. The original socket line helper ( sock_getstrg ) waits for  \n  and hung on a banner or prompt. SSH therefore has its own  ssh_getstrg() : it reads stdout bytes from the queue and returns a partial line at EOF or when the caller's buffer fills, without blocking for a newline that will never come. A seen-but- unconsumed newline is remembered in  nl_pending  (libssh has no  MSG_PEEK ).  reads(s, n)  on a channel is a byte read from the same queue and is the right primitive for an interactive loop. 9.3 ``select()`` readiness select()  on an SSH file must not hang when libssh already buffered the banner during  open() .  ssh_file_pending()  nonblocking-pumps the session and reports ready when stdout (or EOF) is queued. Only stdout matters here: stream  reads()  do not consume stderr/exit chunks, so a nonempty queue of those alone must not make  select()  claim the file is readable.  get_fd()  returns  ssh_get_fd(session)  so the kernel fd participates in the same  select()  set as ordinary sockets. On Windows,  select(&input)  and  Attrib(f, \"tty=raw\")  /  \"tty=sane\"  were added so an interactive client can mux the console with the channel on both Unix and Windows console  iconx . Those tty attributes are general (they apply to  &input ), not SSH-specific. 9.4 ``net::ssh_interactive()`` and ``ussh`` uni/lib/ssh.icn  provides  ssh_interactive(s) : put the local tty in raw mode, mux  &input  with the remote channel via  select() , map lone LF to CRLF when copying remote output to a raw local terminal (needed because raw mode disables  ONLCR ), and restore the tty before returning. It does not close  s . The  ussh  demo ( uni/progs/ussh.icn ) is a small OpenSSH-like client built with the other  uni/progs  demos: interactive shell, or a one-shot remote command via  receive() . s := open(\"user@host\", \"h\", \"key=...\") | stop(&errortext) ssh_interactive(s) close(s) 10. Concurrency and channel lifecycle 10.1 Shared mutex Every  b_file  already has a  mutexid , and all I/O in  fsys.r  already locks around it. When a channel is created via  open(s, \"hc\")  /  open(s, \"hs\") , it does  not  allocate a fresh mutex id -- it copies the session's existing  mutexid . Every channel sharing a session then serializes through the same lock via the existing locking calls in  u_read  /  u_write  /  receive  /  Attrib . libssh sessions are not safe for uncoordinated concurrent access; two threads each holding a different channel on the same session block each other during actual I/O because they contend for the same mutex. 10.2 Child tracking and cascade close A Unicon list of  b_file  pointers was considered so the moving collector would relocate them.  SSHfile  is malloc'd and therefore does not move, so the session owner holds a C linked list of child  SSHfile *  ( children  /  next  /  parent ). No Unicon list is required. close(s)  on the session walks that list, force-closes every remaining channel and SFTP handle (same close-hook logic as an explicit  close() ), marks each child  closed , clears its queue, and then disconnects and frees the session. The child's Unicon  b_file  stays alive until its own  close() , but is unusable immediately -- no dangling libssh objects, no readable leftovers. Closing a channel unlinks it from the owner and frees only that channel. The SFTP subsystem is created lazily, once, on the session owner ( ssh_owner_sftp() ) and shared by every SFTP file or directory on that session. It is freed when the session closes. 10.3 Blocking model I/O uses blocking libssh calls plus Unicon's existing  thread  mechanism -- one thread per in-flight host or channel. Chosen over non-blocking libssh plus  select()  as the  primary  model: it requires no partial-read/retry state machines.  select()  readiness ( ) is implemented so interactive muxing works; it is not a full non-blocking I/O API. 11. SFTP Three shapes, each mapped onto an existing Unicon pattern. 11.1 File transfer -- ``read()`` / ``write()`` c := open(s, \"hs\", \"path=/tmp/firmware.bin\", \"w\") while writes(c, ReadBytes(local_fw)) close(c) Mode  \"hs\"  is SFTP; read/write/append intent comes from the usual mode characters ( r  /  w  /  a  /  b ), either in the same string ( \"hsw\" ) or as a trailing mode-only token.  path=  is required. Backed by  sftp_open()  /  sftp_read()  /  sftp_write()  /  sftp_close() . Structurally a plain file -- another variant feeding the existing dispatch. SFTP regular files are  not  marked  Fs_Socket : they are byte streams, not select-able channels. 11.2 Directory listing Local  open()  on a path that turns out to be a directory transparently switches to  opendir()  /  readdir() , sets  Fs_Directory , and  reads()  yields one entry name per call. SFTP does the same:  open(s, \"hs\", \"path=/tmp/configs\")  checks the remote path with one  sftp_stat() ; if it is a directory,  sftp_opendir()  /  sftp_readdir()  provide the same  Fs_Directory  behavior. 11.3 Metadata -- ``stat()``, ``remove()``, ``rename()`` These extend existing builtins via the leading-optional-file argument idiom, rather than introducing  sftp_remove()  names. The signature change is backward-compatible: a string first argument is still the local-filesystem operation. remove(s, path)  --  sftp_unlink  when  s  is an SSH session or channel file. rename(s, from, to)  --  sftp_rename .  rename  gained a third argument for this form;  rename(s1, s2)  is unchanged. stat(c)  --  sftp_fstat  on an already-open SFTP file. stat(s, path)  -- a single  sftp_stat  request, without paying for  sftp_open()  +  sftp_fstat() . Both entry points are worth supporting rather than just one. if info := stat(s, \"/tmp/firmware.bin\") then    if info.size = expected_size then pull_it() sftp2rec()  populates the same  posix_stat  record as  stat2rec() . SFTP's attribute set is protocol-version dependent; fields the server did not send are left  null  rather than reported as zero, so callers can tell \"absent\" from \"genuinely zero.\" SFTP carries atime/mtime, not ctime. Size, uid/gid (name or numeric), permissions-as-mode-string, and file type ( d  /  l  /  c ) are filled when the corresponding flags are present. 12. Build integration configure --disable-ssh  turns the feature off. Otherwise  CHECK_LIBSSH  in  aclocal.m4  probes for  <libssh/libssh.h>  and  ssh_new , setting  HAVE_LIBSSH . The probe looks under  /usr/local  (FreeBSD) and  /opt/homebrew  (macOS) when those prefixes contain the headers, so later compiles do not miss  libssh/libssh.h .  --with-libssh=DIR  overrides the prefix. A required  --enable-ssh  without the library is a configure error; optional (default) absence degrades gracefully, as with other optional libraries. &features  reports  secure shell  when the library was found.  tests/posix/Makefile  skips  ssh  when the feature is absent. CI installs  libssh-dev  (or the platform equivalent) so the support builds and the deterministic half of the test runs. README install notes list  libssh-dev  /  libssh-devel  /  brew install libssh  alongside the other optional libraries. Guards are  #if HAVE_LIBSSH  throughout  rstructs.h ,  rmacros.h ,  feature.h ,  sys.h ,  fsys.r ,  rposix.r ,  fxposix.ri , and  fmisc.r . 13. Status and remaining work Keyboard-interactive authentication  and an explicit agent attribute. The  publickey_auto  fallback already talks to a running agent. Non-blocking I/O as the primary concurrency model.   select()  readiness is implemented; the I/O calls themselves still block. A  ssh_set_blocking(sess, 0)  plus resume state machine is the obvious next step if thread-count pressure appears. SSH server.  libssh can do it; nothing in the runtime exposes it. The mode character and file-status bit leave room, but the accept/listen path is not designed. Default channel type.  Shell-by-default shipped for mode  \"h\" . Scripted use uses  \"hc\"  with  cmd= . Reconsidering the default remains open and would be a compatibility break. Timeouts.  TCP-connect timeout is the leading integer attribute. There is no separate handshake+auth timeout beyond what that covers, and no exec timeout -- a stuck command blocks until  close() , matching ordinary blocking sockets. Trust-on-first-use.  Verification is check-only ( ssh_session_is_known_server ). A new host fails with 1323 unless the caller uses  h-  or installs the key in  known_hosts  out of band. The runtime does not write the store. ``~`` in ``key=``.  Not expanded. The helper and  ussh  expand  ~/  themselves. Appendix: test coverage tests/posix/ssh.icn  is deterministic without a server: it checks that  &features  reports  secure shell , and that bad attributes,  channel=maybe ,  channel=no  combined with  hc / cmd= ,  cmd=  without mode  c , and a connect to a closed local port all fail with  &errortext  set. A live round-trip runs only when  UNICON_SSH_TESTHOST  is set (host or  user@host ), with optional  UNICON_SSH_TESTPORT ,  UNICON_SSH_TESTKEY ,  UNICON_SSH_KNOWNHOSTS , and  UNICON_SSH_SFTPPATH . The live path covers exec stdout,  Attrib(c, \"exitstatus\") ,  receive()  event tags, partial-line  reads()  of a  printf  with no newline, and SFTP write/read/stat/remove of a binary payload. tests/posix/Makefile  skips the test when the feature is absent. Expected default output is  tests/posix/stand/ssh.std . References ","title":"UTR 26: Native SSH and SFTP Support in Unicon"}];
